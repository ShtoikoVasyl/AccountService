
spring:
  application:
    name: accountservice
  profiles:
    active: dev
  cloud:
    config:
      discovery:
        enabled: true
      name: accountservice
      fail-fast: true
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8008/eureka